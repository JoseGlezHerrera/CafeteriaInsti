<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CafeIES.MAUI.ViewModels"
             xmlns:dto="clr-namespace:CafeIES.Shared.Models;assembly=CafeIES.Shared"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="CafeIES.MAUI.Views.AdminEditProductoPage"
             x:DataType="vm:AdminEditProductoViewModel"
             BackgroundColor="{DynamicResource BgColor}">

    <ContentPage.Resources>
        <toolkit:BoolToObjectConverter x:Key="IsSavingToText"
                                       TrueObject="Guardando..."
                                       FalseObject="Guardar producto"/>
    </ContentPage.Resources>

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior EventName="Appearing" Command="{Binding CargarCommand}"/>
    </ContentPage.Behaviors>

    <Grid RowDefinitions="Auto,*">

        <!-- Header -->
        <Grid Grid.Row="0" BackgroundColor="{DynamicResource SurfaceColor}"
              Padding="16,14" ColumnDefinitions="Auto,*">
            <Button Grid.Column="0"
                    Text="Volver"
                    Style="{StaticResource SecondaryButton}"
                    HeightRequest="38" Padding="12,0"
                    Command="{Binding CancelarCommand}"/>
            <Label Grid.Column="1"
                   Text="{Binding Titulo}"
                   FontFamily="SyneBold" FontSize="20"
                   TextColor="{DynamicResource TextColor}"
                   Margin="12,0,0,0" VerticalOptions="Center"/>
        </Grid>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="24,20" Spacing="18">

                <!-- Nombre -->
                <VerticalStackLayout Spacing="6">
                    <Label Text="Nombre"
                           FontFamily="SyneBold" FontSize="13"
                           TextColor="{DynamicResource MutedColor}"/>
                    <Border BackgroundColor="{DynamicResource Surface2Color}"
                            StrokeThickness="1" Stroke="{DynamicResource BorderColor}">
                        <Border.StrokeShape><RoundRectangle CornerRadius="14"/></Border.StrokeShape>
                        <Entry Style="{StaticResource InputEntry}"
                               Text="{Binding Nombre}"
                               Placeholder="ej. Bocadillo Mixto"
                               Margin="4,0"/>
                    </Border>
                </VerticalStackLayout>

                <!-- Descripcion -->
                <VerticalStackLayout Spacing="6">
                    <Label Text="Descripcion (opcional)"
                           FontFamily="SyneBold" FontSize="13"
                           TextColor="{DynamicResource MutedColor}"/>
                    <Border BackgroundColor="{DynamicResource Surface2Color}"
                            StrokeThickness="1" Stroke="{DynamicResource BorderColor}">
                        <Border.StrokeShape><RoundRectangle CornerRadius="14"/></Border.StrokeShape>
                        <Entry Style="{StaticResource InputEntry}"
                               Text="{Binding Descripcion}"
                               Placeholder="Breve descripcion"
                               Margin="4,0"/>
                    </Border>
                </VerticalStackLayout>

                <!-- Precio y Stock -->
                <Grid ColumnDefinitions="*,16,*">
                    <VerticalStackLayout Grid.Column="0" Spacing="6">
                        <Label Text="Precio (EUR)"
                               FontFamily="SyneBold" FontSize="13"
                               TextColor="{DynamicResource MutedColor}"/>
                        <Border BackgroundColor="{DynamicResource Surface2Color}"
                                StrokeThickness="1" Stroke="{DynamicResource BorderColor}">
                            <Border.StrokeShape><RoundRectangle CornerRadius="14"/></Border.StrokeShape>
                            <Entry Style="{StaticResource InputEntry}"
                                   Text="{Binding Precio}"
                                   Keyboard="Numeric" Placeholder="0.00"
                                   Margin="4,0"/>
                        </Border>
                    </VerticalStackLayout>
                    <VerticalStackLayout Grid.Column="2" Spacing="6">
                        <Label Text="Stock (-1 = ilimitado)"
                               FontFamily="SyneBold" FontSize="13"
                               TextColor="{DynamicResource MutedColor}"/>
                        <Border BackgroundColor="{DynamicResource Surface2Color}"
                                StrokeThickness="1" Stroke="{DynamicResource BorderColor}">
                            <Border.StrokeShape><RoundRectangle CornerRadius="14"/></Border.StrokeShape>
                            <Entry Style="{StaticResource InputEntry}"
                                   Text="{Binding Stock}"
                                   Keyboard="Numeric" Placeholder="-1"
                                   Margin="4,0"/>
                        </Border>
                    </VerticalStackLayout>
                </Grid>

                <!-- Imagen -->
                <VerticalStackLayout Spacing="6">
                    <Label Text="Imagen del producto"
                           FontFamily="SyneBold" FontSize="13"
                           TextColor="{DynamicResource MutedColor}"/>
                    <Grid ColumnDefinitions="80,*" ColumnSpacing="14">
                        <!-- Preview -->
                        <Border Grid.Column="0"
                                WidthRequest="72" HeightRequest="72"
                                BackgroundColor="{DynamicResource Surface2Color}"
                                StrokeThickness="1" Stroke="{DynamicResource BorderColor}">
                            <Border.StrokeShape><RoundRectangle CornerRadius="14"/></Border.StrokeShape>
                            <!-- Imagen cargada -->
                            <Image Source="{Binding ImagenUrl}"
                                   Aspect="AspectFill"
                                   IsVisible="{Binding TieneImagen}"/>
                            <!-- Placeholder sin imagen -->
                            <Label Text="📷" FontSize="30"
                                   HorizontalOptions="Center" VerticalOptions="Center"
                                   IsVisible="{Binding TieneImagen, Converter={StaticResource InvertBoolConverter}}"/>
                        </Border>
                        <!-- Botón seleccionar -->
                        <VerticalStackLayout Grid.Column="1" Spacing="6" VerticalOptions="Center">
                            <Grid>
                                <Button Text="📷 Seleccionar imagen"
                                        Style="{StaticResource SecondaryButton}"
                                        HeightRequest="40"
                                        Command="{Binding SeleccionarImagenCommand}"
                                        IsEnabled="{Binding IsSubiendoImagen, Converter={StaticResource InvertBoolConverter}}"
                                        IsVisible="{Binding IsSubiendoImagen, Converter={StaticResource InvertBoolConverter}}"/>
                                <ActivityIndicator IsRunning="{Binding IsSubiendoImagen}"
                                                   IsVisible="{Binding IsSubiendoImagen}"
                                                   Color="{StaticResource AccentColor}"
                                                   HeightRequest="40"/>
                            </Grid>
                            <Label Text="JPG, PNG o WebP · máx. 5 MB"
                                   FontFamily="DMSans" FontSize="11"
                                   TextColor="{DynamicResource MutedColor}"/>
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>

                <!-- Categoria -->
                <VerticalStackLayout Spacing="6">
                    <Label Text="Categoria"
                           FontFamily="SyneBold" FontSize="13"
                           TextColor="{DynamicResource MutedColor}"/>
                    <Border BackgroundColor="{DynamicResource Surface2Color}"
                            StrokeThickness="1" Stroke="{DynamicResource BorderColor}">
                        <Border.StrokeShape><RoundRectangle CornerRadius="14"/></Border.StrokeShape>
                        <Picker ItemsSource="{Binding Categorias}"
                                SelectedItem="{Binding CategoriaSeleccionada}"
                                ItemDisplayBinding="{Binding Nombre, x:DataType=dto:CategoriaDto}"
                                TextColor="{DynamicResource TextColor}"
                                TitleColor="{DynamicResource MutedColor}"
                                Title="Seleccionar categoria"
                                BackgroundColor="Transparent"
                                Margin="8,4"/>
                    </Border>
                </VerticalStackLayout>

                <!-- Desayuno gratuito -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Desayuno gratuito 🍊"
                           FontFamily="SyneBold" FontSize="13"
                           TextColor="{DynamicResource MutedColor}"/>
                    <Grid ColumnDefinitions="*,8,*,8,*" RowDefinitions="Auto">

                        <!-- Ninguno -->
                        <Border Grid.Column="0"
                                Padding="0,10"
                                StrokeThickness="2">
                            <Border.StrokeShape><RoundRectangle CornerRadius="14"/></Border.StrokeShape>
                            <Border.Triggers>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding ComponenteDesayuno}"
                                             Value="Ninguno">
                                    <Setter Property="BackgroundColor" Value="{DynamicResource Surface2Color}"/>
                                    <Setter Property="Stroke" Value="{StaticResource AccentColor}"/>
                                </DataTrigger>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding ComponenteDesayuno}"
                                             Value="Zumo">
                                    <Setter Property="BackgroundColor" Value="{DynamicResource Surface2Color}"/>
                                    <Setter Property="Stroke" Value="{DynamicResource BorderColor}"/>
                                </DataTrigger>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding ComponenteDesayuno}"
                                             Value="Bocata">
                                    <Setter Property="BackgroundColor" Value="{DynamicResource Surface2Color}"/>
                                    <Setter Property="Stroke" Value="{DynamicResource BorderColor}"/>
                                </DataTrigger>
                            </Border.Triggers>
                            <VerticalStackLayout Spacing="2" HorizontalOptions="Center">
                                <Label Text="❌" FontSize="20" HorizontalOptions="Center"/>
                                <Label Text="Ninguno" FontFamily="SyneBold" FontSize="11"
                                       TextColor="{DynamicResource TextColor}" HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetComponenteDesayunoCommand}"
                                                      CommandParameter="0"/>
                            </Border.GestureRecognizers>
                        </Border>

                        <!-- Zumo -->
                        <Border Grid.Column="2"
                                Padding="0,10"
                                StrokeThickness="2">
                            <Border.StrokeShape><RoundRectangle CornerRadius="14"/></Border.StrokeShape>
                            <Border.Triggers>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding ComponenteDesayuno}"
                                             Value="Zumo">
                                    <Setter Property="BackgroundColor" Value="#1a4fa3e8"/>
                                    <Setter Property="Stroke" Value="#4fa3e8"/>
                                </DataTrigger>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding ComponenteDesayuno}"
                                             Value="Ninguno">
                                    <Setter Property="BackgroundColor" Value="{DynamicResource Surface2Color}"/>
                                    <Setter Property="Stroke" Value="{DynamicResource BorderColor}"/>
                                </DataTrigger>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding ComponenteDesayuno}"
                                             Value="Bocata">
                                    <Setter Property="BackgroundColor" Value="{DynamicResource Surface2Color}"/>
                                    <Setter Property="Stroke" Value="{DynamicResource BorderColor}"/>
                                </DataTrigger>
                            </Border.Triggers>
                            <VerticalStackLayout Spacing="2" HorizontalOptions="Center">
                                <Label Text="🥤" FontSize="20" HorizontalOptions="Center"/>
                                <Label Text="Zumo" FontFamily="SyneBold" FontSize="11"
                                       TextColor="{DynamicResource TextColor}" HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetComponenteDesayunoCommand}"
                                                      CommandParameter="1"/>
                            </Border.GestureRecognizers>
                        </Border>

                        <!-- Bocata -->
                        <Border Grid.Column="4"
                                Padding="0,10"
                                StrokeThickness="2">
                            <Border.StrokeShape><RoundRectangle CornerRadius="14"/></Border.StrokeShape>
                            <Border.Triggers>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding ComponenteDesayuno}"
                                             Value="Bocata">
                                    <Setter Property="BackgroundColor" Value="#1a4caf82"/>
                                    <Setter Property="Stroke" Value="#4caf82"/>
                                </DataTrigger>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding ComponenteDesayuno}"
                                             Value="Ninguno">
                                    <Setter Property="BackgroundColor" Value="{DynamicResource Surface2Color}"/>
                                    <Setter Property="Stroke" Value="{DynamicResource BorderColor}"/>
                                </DataTrigger>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding ComponenteDesayuno}"
                                             Value="Zumo">
                                    <Setter Property="BackgroundColor" Value="{DynamicResource Surface2Color}"/>
                                    <Setter Property="Stroke" Value="{DynamicResource BorderColor}"/>
                                </DataTrigger>
                            </Border.Triggers>
                            <VerticalStackLayout Spacing="2" HorizontalOptions="Center">
                                <Label Text="🥪" FontSize="20" HorizontalOptions="Center"/>
                                <Label Text="Bocata" FontFamily="SyneBold" FontSize="11"
                                       TextColor="{DynamicResource TextColor}" HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetComponenteDesayunoCommand}"
                                                      CommandParameter="2"/>
                            </Border.GestureRecognizers>
                        </Border>

                    </Grid>
                </VerticalStackLayout>

                <!-- Alérgenos -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Alérgenos"
                           FontFamily="SyneBold" FontSize="13"
                           TextColor="{DynamicResource MutedColor}"/>
                    <CollectionView ItemsSource="{Binding Alergenos}"
                                    SelectionMode="None">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Vertical" Span="2" VerticalItemSpacing="4" HorizontalItemSpacing="8"/>
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="{x:Type vm:AlergenoSeleccionable}">
                                <HorizontalStackLayout Spacing="8" Padding="0,2">
                                    <CheckBox IsChecked="{Binding Seleccionado}" Color="{StaticResource AccentColor}"/>
                                    <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                        <Label Text="{Binding Alergeno.Emoji}" FontSize="16" VerticalOptions="Center"/>
                                        <Label Text="{Binding Alergeno.Nombre}" FontFamily="DMSans" FontSize="13"
                                               TextColor="{DynamicResource TextColor}" VerticalOptions="Center"/>
                                    </HorizontalStackLayout>
                                </HorizontalStackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>

                <!-- Ingredientes personalizables -->
                <VerticalStackLayout Spacing="8" IsVisible="{Binding TieneIngredientes}">
                    <Label Text="Ingredientes personalizables"
                           FontFamily="SyneBold" FontSize="13"
                           TextColor="{DynamicResource MutedColor}"/>
                    <CollectionView ItemsSource="{Binding Ingredientes}" SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="{x:Type vm:IngredienteSeleccionable}">
                                <Border Style="{StaticResource Card}" Padding="12,10" Margin="0,0,0,6">
                                    <VerticalStackLayout Spacing="8">

                                        <!-- Fila principal: switch + emoji + nombre + precio -->
                                        <Grid ColumnDefinitions="Auto,Auto,*,Auto" ColumnSpacing="8">
                                            <Switch Grid.Column="0"
                                                    IsToggled="{Binding Seleccionado}"
                                                    OnColor="{StaticResource AccentColor}"
                                                    VerticalOptions="Center"/>
                                            <Label Grid.Column="1"
                                                   Text="{Binding Ingrediente.Emoji}"
                                                   FontSize="20" VerticalOptions="Center"/>
                                            <Label Grid.Column="2"
                                                   Text="{Binding Ingrediente.Nombre}"
                                                   FontFamily="SyneBold" FontSize="13"
                                                   TextColor="{DynamicResource TextColor}"
                                                   VerticalOptions="Center"/>
                                            <Label Grid.Column="3"
                                                   IsVisible="{Binding IngredienteConPrecio}"
                                                   FontFamily="DMSans" FontSize="12"
                                                   TextColor="{DynamicResource MutedColor}"
                                                   VerticalOptions="Center">
                                                <Label.FormattedText>
                                                    <FormattedString>
                                                        <Span Text="+"/>
                                                        <Span Text="{Binding Ingrediente.PrecioExtra, StringFormat='{0:F2}'}"/>
                                                        <Span Text="€"/>
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>
                                        </Grid>

                                        <!-- Configuración (solo cuando está asignado) -->
                                        <VerticalStackLayout IsVisible="{Binding Seleccionado}" Spacing="6">
                                            <!-- Fila 1: checkboxes Base / Quitable -->
                                            <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                                                <HorizontalStackLayout Grid.Column="0" Spacing="4" VerticalOptions="Center">
                                                    <CheckBox IsChecked="{Binding EsBase}"
                                                              Color="{StaticResource AccentColor}"/>
                                                    <Label Text="Base" FontFamily="DMSans" FontSize="12"
                                                           TextColor="{DynamicResource TextColor}"
                                                           VerticalOptions="Center"/>
                                                </HorizontalStackLayout>
                                                <HorizontalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                                                    <CheckBox IsChecked="{Binding EsQuitable}"
                                                              Color="{StaticResource AccentColor}"/>
                                                    <Label Text="Quitable" FontFamily="DMSans" FontSize="12"
                                                           TextColor="{DynamicResource TextColor}"
                                                           VerticalOptions="Center"/>
                                                </HorizontalStackLayout>
                                            </Grid>
                                            <!-- Fila 2: CantidadMaxima -->
                                            <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                                <Label Text="Máx. unidades" FontFamily="DMSans" FontSize="12"
                                                       TextColor="{DynamicResource MutedColor}"
                                                       VerticalOptions="Center"/>
                                                <Border BackgroundColor="{DynamicResource Surface2Color}"
                                                        StrokeThickness="1" Stroke="{DynamicResource BorderColor}"
                                                        WidthRequest="54">
                                                    <Border.StrokeShape><RoundRectangle CornerRadius="8"/></Border.StrokeShape>
                                                    <Entry Text="{Binding CantidadMaxima}" Keyboard="Numeric"
                                                           FontSize="13" Margin="4,0" HeightRequest="36"/>
                                                </Border>
                                            </HorizontalStackLayout>
                                        </VerticalStackLayout>

                                    </VerticalStackLayout>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                    <Label Text="Base = incluido por defecto · Quitable = el cliente puede quitarlo · Máx. unidades = 1 switch, &gt;1 activa el stepper."
                           FontFamily="DMSans" FontSize="11"
                           TextColor="{DynamicResource MutedColor}"/>
                </VerticalStackLayout>

                <!-- Mensaje de error -->
                <Border BackgroundColor="#1ae05252"
                        StrokeThickness="1" Stroke="#40e05252"
                        Padding="14,10"
                        IsVisible="{Binding HasError}">
                    <Border.StrokeShape><RoundRectangle CornerRadius="12"/></Border.StrokeShape>
                    <Label Text="{Binding Error}"
                           FontFamily="DMSans" FontSize="13"
                           TextColor="#e05252"/>
                </Border>

                <!-- Boton guardar -->
                <Button Style="{StaticResource PrimaryButton}"
                        Text="{Binding IsSaving, Converter={StaticResource IsSavingToText}}"
                        Command="{Binding GuardarCommand}"
                        IsEnabled="{Binding IsSaving, Converter={StaticResource InvertBoolConverter}}"
                        Margin="0,8,0,0"/>

            </VerticalStackLayout>
        </ScrollView>

    </Grid>
</ContentPage>
